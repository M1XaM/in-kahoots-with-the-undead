name: Player Registered
$kind: http-request
method: POST
url: "http://localhost:8086/resources/_mock/events/player.registered"
description: "Simulates player.registered: creates zero balances for player 42."
headers:
  - key: Content-Type
    value: "application/json"
  - key: X-Internal-Key
    value: "changeme"
body:
  type: json
  content: |
    {
      "eventId": "{{$guid}}",
      "type": "PlayerRegistered",
      "version": 1,
      "occurredAt": "2026-09-25T10:00:00Z",
      "payload": {
        "playerId": 42,
        "username": "razvan"
      }
    }
