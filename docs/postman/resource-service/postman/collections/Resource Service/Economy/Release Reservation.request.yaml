name: Release Reservation
$kind: http-request
method: DELETE
url: "http://localhost:8086/resources/reservations/{{reservationId}}"
description: "409 ALREADY_COMMITTED after a commit; reserve again to see 204."
headers:
  - key: X-Internal-Key
    value: "changeme"
