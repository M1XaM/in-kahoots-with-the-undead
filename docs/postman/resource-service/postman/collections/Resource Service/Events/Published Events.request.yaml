name: Published Events
$kind: http-request
method: GET
url: "http://localhost:8086/resources/internal/events?since=0"
description: "Outbox: the events /events subscribers receive (resource.gathered)."
headers:
  - key: X-Internal-Key
    value: "changeme"
